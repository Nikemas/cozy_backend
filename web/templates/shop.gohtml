{{define "content"}}
<div class="screen-placeholder">
  <h1>{{t "screen.shop.title"}}</h1>
  <p class="muted">{{t "common.soon"}}</p>
  <!-- TODO(catalog): product grid + filters/search via HTMX partial-swap (Task 2) -->
</div>
{{end}}
